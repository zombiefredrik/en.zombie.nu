# to exclude Obsidian's settings (including plugin and hotkey configurations)
.obsidian/

# to only exclude plugin configuration. Might be useful to prevent some plugin from exposing sensitive data
.obsidian/plugins

# OR only to exclude workspace cache
.obsidian/workspace.json

# to exclude workspace cache specific to mobile devices
.obsidian/workspace-mobile.json

# Add below lines to exclude OS settings and caches
.trash/
.DS_Store

# Hugo build files (vi VILL ha public/ i Git för servern)
website/.hugo_build.lock
website/resources/

# Node modules om Hugo-tema har sådana
website/node_modules/

# Lokala Hugo-drafts och temp-filer
website/content/**/.DS_Store

# Backup-filer
*.bak
*.tmp
*~